def swap(array, x, y)
    temp = array[x]
    array[x] = array[y]
    array[y] = temp
end

def ssort(array)
    i = 0
    j = 1
    while i < array.length - 1
        while j < array.length
            
        end
    end
end
